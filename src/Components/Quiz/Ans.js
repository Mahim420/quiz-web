import React from 'react';

const Ans = ({ans}) => {
    return (
        <div className='border ml-3 mt-3 border-4 border-blue-600'>
           <p>{ans}</p>
        </div>
    );
};

export default Ans;